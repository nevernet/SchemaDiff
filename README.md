# SchemaDiff

SQL Schema 对比工具 - 比较两个 SQL 文件或目录，生成 Migration 脚本。

## 功能特性

- 🔍 **Schema 对比** - 比较两个数据库 schema 的差异
- 📝 **Migration 生成** - 自动生成 ALTER 语句
- 📁 **目录支持** - 支持目录级别的批量对比
- 🗄️ **多方言支持** - MySQL / PostgreSQL

## 安装

```bash
pip install sqlglot
```

## 使用方法

### 单文件对比

```bash
python main.py <源SQL文件> <目标SQL文件>
```

### 目录对比

```bash
python main.py <源目录> <目标目录>
```

### 示例

```bash
# 对比两个 SQL 文件
python main.py SQL/users_v1.sql SQL/users_v2.sql

# 对比两个目录
python main.py backup/20260301 SQL
```

## 测试样本

### SQL/users_v1.sql (初始版本)

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE
);
```

### SQL/users_v2.sql (新增字段)

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    phone VARCHAR(20),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### SQL/orders.sql (含外键)

```sql
CREATE TABLE orders (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT,
    amount DECIMAL(10,2),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

## 测试结果

### 测试1: 单文件对比

```bash
$ python main.py SQL/users_v1.sql SQL/users_v2.sql
```

**输出:**
```
Parsing source file: SQL/users_v1.sql
  Found 1 tables
Parsing target file: SQL/users_v2.sql
  Found 1 tables

Comparing schemas...
  Found 2 changes

Changes detected:
  + ADD COLUMN: users.phone
  + ADD COLUMN: users.created_at

--- Migration Script ---
-- Migration generated by SchemaDiff
-- Generated at: 2026-03-01 23:44:28.113326

BEGIN;
ALTER TABLE `users` ADD COLUMN `phone` ;
ALTER TABLE `users` ADD COLUMN `created_at` ;
COMMIT;
```

### 测试2: 目录对比

```bash
$ python main.py backup/20260301 SQL
```

## 支持的 SQL 特性

| 特性 | 状态 |
|------|------|
| CREATE TABLE | ✅ |
| ADD COLUMN | ✅ |
| DROP COLUMN | ✅ |
| ALTER COLUMN | ✅ |
| PRIMARY KEY | ✅ |
| FOREIGN KEY | ✅ |
| UNIQUE INDEX | ✅ |
| DEFAULT VALUE | ✅ |
| AUTO_INCREMENT | ✅ |

## 输出格式

Migration 脚本包含：
- BEGIN / COMMIT 事务包装
- CREATE TABLE
- ALTER TABLE ADD/DROP/MODIFY COLUMN
- CREATE/DROP INDEX
- ADD/DROP FOREIGN KEY

## 技术栈

- Python 3
- sqlglot (SQL 解析)

## 许可证

MIT
